apiVersion: apps/v1
kind: Deployment
metadata:
  name: result-deployment
spec:
  selector:
    matchLabels:
      app: result
  template:
    metadata:
      labels:
        app: result
    spec:
      containers:
      - name: result-container
        image: fatemehparmoo/result:latest
        ports:
        - containerPort: 80
        env:
        - name: PORT
          value: "80"

        - name: PG_HOST
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: DB_HOST

        - name: PG_DATABASE
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: DB_NAME

        - name: PG_USER
          valueFrom:
            secretKeyRef:
              name: app-secret
              key: DB_USERNAME


        - name: PG_PASSWORD
          valueFrom:
            secretKeyRef:
              name: app-secret
              key: DB_PASSWORD

        - name: PG_PORT
          value: "5432"

---
apiVersion: v1
kind: Service
metadata:
  name: result-service
spec:
  selector:
    app: result
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP